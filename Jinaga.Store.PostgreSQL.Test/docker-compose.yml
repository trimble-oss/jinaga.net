services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: jinaga
      POSTGRES_PASSWORD: jinaga_test
      POSTGRES_DB: jinaga_test
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U jinaga -d jinaga_test"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
